#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../openbadges-bakery/bin/oven" "$@"
  ret=$?
else 
  node  "$basedir/../openbadges-bakery/bin/oven" "$@"
  ret=$?
fi
exit $ret
